export class Ingredient {
    constructor(public name: string, public amount: number) { // public is a shortcut to create properties and assign them to the values that are passed into the constructor
    }
}